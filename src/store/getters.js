export const user = state => state.user;

export const addressList = state => state.addressList;

export const currentAddrCode = state => state.currentAddrCode;

export const orderList = state => state.orderList;

export const bankcardList = state => state.bankcardList;

export const currentBankCode = state => state.currentBankCode;

export const cnyAccount = state => state.cnyAccount;

export const jfAccount = state => state.jfAccount;

export const jyAccount = state => state.jyAccount;

export const currentMaterial = state => state.currentMaterial;

export const currentModel = state => state.currentModel;

export const currentOrder = state => state.currentOrder;

export const currentArticle = state => state.currentArticle;
